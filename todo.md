# Execution Tracking Has Moved

This repository no longer uses `todo.md` as the source of truth for execution work.

Use GitHub Issues for:

* tasks
* bugs
* follow-up work
* validation findings
* roadmap slices that need implementation

`plan.md` now defines mission, vision, and goals.

This file remains only as a compatibility pointer and should not be expanded.
